spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myblog
    username: root
    password: test

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

server:
  port: 8082


app:
  jwt-secret: JwtSecretKey
  jwt-expiration-milliSeconds: 1100000
